# 个人项目信息管理系统 · 设计文档（v1.0.0 个人归档版）

> **版本**：v1.0.0（个人归档版）
> **本版变更**：定位从"项目完成度 + 审核流转 + 多版本"改为"**归档已完成文件、分类管理**"。
> 删除：文件状态流转、必要性、版本号、进度、固定格式枚举、期望格式、"填模板生成文档"流程。
> 新增：插槽可嵌套（多级树）、文件自定义标签、动态格式识别（无固定枚举）、结构模板（阶段 + 插槽树，无模板文件）。
> 软件版本 `0.4.0 → 1.0.0`（数据模型大改，不兼容旧版）。
>
> **文档性质**：**总方向指针 / 单一事实源（Single Source of Truth）**
> **核心设计原则**：**灵活性第一 + 无死代码**。架构通用、数据外置、格式动态识别（任意格式都成立，不做固定枚举）；不预置业务内容。
> **语言约定**：全界面**简体中文**；品牌缩写 `PPIMS`；格式/标签等一律显示中文或扩展名，不露出内部枚举码。

---

## 目录

0. [如何用本文档（必读）](#〇如何用本文档必读)
1. [技术栈](#一技术栈)
2. [项目架构与数据模型](#二项目架构与数据模型)
3. [功能清单与验收](#三功能清单与验收)
4. [扩展延伸](#四扩展延伸)
5. [附录：设计决策记录（ADR）](#附录设计决策记录adr-摘要)

---

## 〇、如何用本文档（必读）

> 本文档是**整个项目的唯一方向基准**。一切以本文档为准。

**硬性约定：**
1. **本文档即契约**：实现前先核对本文档；冲突时以本文档为准；认为文档有误，先修订文档再改代码。
2. **不得偏离已定决策**：技术栈、数据模型、编辑引擎路线是已拍板的，不得擅自更换。
3. **无死代码**：格式识别用动态代码（任意格式都成立），**不做固定枚举**；禁止无用的占位/死分支/死文件。
4. **保持灵活性**：新增功能按可插拔模块接入，不得为某个功能破坏通用架构。
5. **文档随实现演进**：每完成一个功能，回来更新对应章节。

---

## 一、技术栈

> 选型原则：**本地优先、离线可用、不依赖云服务、不跳转第三方编辑**。格式保真与中文健壮性为硬指标。

### 1.1 运行框架

| 层次 | 选型 | 说明 |
|------|------|------|
| 桌面壳 | **Electron** | 跨平台，可调用本地文件；个人本地工具首选 |
| 前端界面 | **Vue 3 + TypeScript + Vite** | 组合式 API，组件化 |
| UI 组件库 | **Element Plus** | 中文生态成熟，表单/表格齐全 |
| 状态管理 | **Pinia** | 轻量，管理项目/插槽树/文件全局状态 |

### 1.2 数据与文件

| 能力 | 选型 | 说明 |
|------|------|------|
| 项目元数据 | **项目内 `project.json`** | 文件夹自包含、可整体搬移；不依赖外部数据库 |
| 本地文件操作 | **Node.js `fs`** | 复制、打包、目录 |
| 批量打包 | **`archiver`** | 导出 ZIP（扩展） |

### 1.3 文档编辑引擎（核心，保真关键）

> **保真策略**：**全程在原始文档上"原位编辑"，绝不重新序列化文档**。

| 能力 | 选型 | 说明 |
|------|------|------|
| Word 原位编辑 | **JSZip + 原生 `document.xml` 操作** | 打开 docx(zip) → 只改 `<w:t>` 文本 → 塞回原 zip；样式/字体/表格零改动 |
| Excel 原位编辑 | **SheetJS（xlsx）** | 程序内单元格编辑，保留工作簿结构 |
| CSV 原位编辑 | **自研纯文本引擎** | 解析二维网格（处理引号内逗号）→ 改单元格 → 序列化写回 |
| 中文处理 | 全链路 **UTF-8 原生** | CJK 全角/半角、混排换行；字段识别兼容中文标签与全角冒号 |

> **能力边界（如实标注）**：
> - ✅ **改已有文字 / 改表格单元格**：格式 100% 保真（只换文本，不动样式）。
> - ⚠️ **docx 重复文本替换**：字面替换，重复文本时可能命中错处（如实上报 missed）。
> - ⚠️ **老式 doc/xls（二进制）**：应用内**查看**（详情 + 系统程序打开）；不承诺原位编辑。

### 1.4 格式识别（动态、无枚举、无死代码）

> **核心原则**：格式 = 文件扩展名（小写）。**任意扩展名都成立**，不做固定枚举。
> 例：`报告.DOCX` → `docx`；`图纸.dwg` → `dwg`；`压缩包.zip` → `zip`。
> 上传什么格式就是什么格式，软件不为任何格式写死代码。

**编辑/预览能力判定**（"用哪个引擎"，非格式枚举）：
- 可应用内原位编辑：`csv / docx / docm / dotx / xlsx / xlsm / xltm`
- 老式二进制（查看 + 系统打开）：`doc / xls`
- 其他格式：查看（详情卡片）+ 系统程序打开 + 下载

---

## 二、项目架构与数据模型

### 2.1 目录结构（代码）

```
PPIMS/
  core/                 ← 核心管理层（类型/store/服务/IPC）
    types.ts            ← 数据模型（单一事实源）
    util.ts             ← 纯工具（动态格式/重名加序号/大小）
    classify.ts         ← 分类纯逻辑
    template-mapping.ts ← 项目→结构模板映射
    stores/             ← Pinia store（project/app）
    services/           ← docx-engine / xlsx-engine / csv-engine / fs / template-service
    ipc.ts / preload.ts / api.d.ts / main.ts
  features/             ← 功能层（Vue 组件）
    RootSetup / ProjectList / ProjectDetail / SlotTreePanel /
    SlotWorkspace / FilePanel / FileEditor / TemplateManager /
    SearchPanel / SlotStructureEditor
  dev/
    test/               ← unit.test.ts（vitest）+ run-standalone.ts（Node 原生）
    scripts/            ← build-electron / gen-icon / derive-icons
  docs/                 ← 本文档
  README.md
```

### 2.2 数据模型（v1.0.0）

```
Project
├─ info: ProjectInfo         // 名称/编号/地区/阶段/类型/下发时间/备注/分类取值
├─ rootPath                  // 本机绝对路径（不入 project.json 关键数据）
├─ slots: Slot[]             // 顶层插槽（= 阶段）
├─ schemaVersion: 2          // v1.0.0 新数据模型
└─ createdAt / updatedAt

Slot（统一节点，可嵌套成多级树）
├─ id / name / order
├─ files: FileEntry[]        // 插槽下的文件（1..n，各自独立命名）
└─ subSlots: Slot[]          // 子插槽（多级树）★

FileEntry（独立文件，无版本/无状态）
├─ id
├─ name                      // 显示名（重名自动加序号：任务书 → 任务书_2）
├─ format                    // 动态格式（扩展名，非枚举）
├─ size                      // 字节（自动）
├─ createdAt                 // 上传时间（自动）
├─ updatedAt?                // 编辑后保存时间
├─ path                      // 相对项目根 POSIX 路径（files/xxx）
├─ remark?                   // 描述/备注
└─ tags: string[]            // 自定义标签（"重要""已核"…）★

StructureTemplate（结构模板 = 阶段 + 插槽树，无模板文件）
├─ id / name / description
├─ structure: TplSlotInput[] // 纯结构：name + subSlots（无 id/files/order）
└─ createdAt / updatedAt
```

**v1.0.0 删除的类型/常量**：`FileStatus`、`Necessity`、`FileFormat`(枚举)、`QUALIFIED_STATUSES`、`NECESSITY_WEIGHT`、`STATUS_LABEL`、`NECESSITY_LABEL`、`FORMAT_LABEL`、`FileInstance`、`Stage`、`Template`、`ProjectTemplate`、`TplStageInput`、`progress`/`stageProgress`/`projectProgress`、`PRESET_STAGES`、`SCHEMA_VERSION=1`。

### 2.3 存储模型（文件物理存放）

```
项目根/
  project.json          ← 插槽树 + 文件清单（单一事实源）
  files/                ← 所有上传的文件（扁平存放）
    <文件名>             ← 按"自动编号后的显示名 + 扩展名"命名
```

> 扁平化 `files/` 便于整体搬移，避免多级目录重命名/移动复杂度。
> "插槽 → 文件"的归属关系记录在 `project.json` 的 FileEntry 里。

### 2.4 分类维度（根级配置）

- 维度是项目的**分类字段**（如"地区""专业""客户"），只用于列表**分组/筛选/排序**。
- 维度**用户自定义、可扩展**（`dim_` 前缀 + 稳定 id），取值自由文本。
- 系统**不预置业务维度**，由用户按需定义（存 `<root>/ppims.json`）。

---

## 三、功能清单与验收

### 3.1 插槽树（核心导航）

| 功能 | 说明 | 验收 |
|------|------|------|
| 顶层插槽（= 阶段） | 建/删/改名/调序 | 能增删改排序 |
| 子插槽（嵌套） | 在任意插槽下加子插槽，形成多级树 | 能嵌套多级 |
| 选中联动 | 树面板/子插槽跳转/检索共用 `selectedSlotId` | 干净联动，无脏 emit |

### 3.2 文件管理

| 功能 | 说明 | 验收 |
|------|------|------|
| 多文件上传 | 一次传多个，重名自动加序号 | 批量上传正确 |
| 动态格式 | 扩展名即格式，任意格式 | pdf/zip/csv/dwg 都正确 |
| 自定义标签 | 加"重要/已核"等标签，点击移除 | 标签可增删 |
| 大小/日期 | 自动显示 | 正确显示 |
| 改名/删除 | 文件改名；删除记录 + 物理文件 | 正确 |
| 下载/外部打开 | 下载到选定位置；系统程序打开 | 正确 |

### 3.3 原位编辑（路径 C）

| 格式 | 能力 | 验收 |
|------|------|------|
| csv | 表格单元格原位编辑 | 改单元格写回，其余不变 |
| xlsx/xlsm/xltm | 单元格原位编辑 | 改单元格写回，保留工作簿 |
| docx/docm/dotx | 段落文本原位编辑 | 改文本写回，样式零改动 |
| doc/xls | 查看（详情 + 系统打开） | 能查看、能打开 |
| 其他 | 查看 + 系统打开 + 下载 | 能查看、能打开 |

### 3.4 结构模板

| 功能 | 说明 | 验收 |
|------|------|------|
| 新建/编辑 | 名称 + 插槽树（可嵌套） | 能搭结构 |
| 从项目另存 | 抓"阶段 + 插槽"树（无文件） | 正确捕获 |
| 复制/删除 | 整模板复制/删除 | 正确 |
| 建项套用 | 一键生成"阶段 + 插槽"树 | 免逐一手填 |

### 3.5 检索

| 功能 | 说明 | 验收 |
|------|------|------|
| 关键词 | 文件名 / 插槽路径 | 命中正确 |
| 筛选 | 顶层插槽 / 动态格式 | 筛选正确 |
| 跳转 | 点击结果打开对应插槽 | 联动正确 |

### 3.6 分类维度（根级）

| 功能 | 说明 | 验收 |
|------|------|------|
| 自定义维度 | 加/删/改名维度 | 正确 |
| 分组/筛选/排序 | 按维度分组列表 | 正确 |
| 项目分类取值 | 建项/编辑时填取值 | 正确 |

---

## 四、扩展延伸

| 方向 | 说明 |
|------|------|
| 批量打包导出 | 按插槽/按项目导出 ZIP（`archiver`） |
| 全文检索 | SQLite FTS5 跨文件内容检索（当前是文件级索引） |
| 老式 doc/xls 原位编辑 | 接入 OnlyOffice/Word 兜底层（可选） |
| 多格式预览 | 图片/CAD/PDF 应用内预览（当前是系统打开） |
| 备份/同步 | 项目文件夹整体备份（当前靠用户复制） |

---

## 附录：设计决策记录（ADR 摘要）

| ADR | 决策 | 理由 |
|-----|------|------|
| 1 | 定位改为个人归档版 | 用户实际需求是"归档已完成文件"，非"跟踪完成度" |
| 2 | 插槽统一为可嵌套树（阶段 = 顶层插槽） | 用户要求"插槽可嵌套"，阶段与插槽统一更简洁 |
| 3 | 文件删除状态/版本/必要性 | 归档场景不需要流转；多文件各自独立命名 |
| 4 | 格式动态识别（扩展名即格式） | 用户要求"识别所有格式、不做固定枚举、无死代码" |
| 5 | 原位编辑用 XML/单元格字面替换 | 保真最强（只换文本，不动样式）；中文零改动 |
| 6 | doc/xls 只查看不编辑 | 二进制格式原位编辑需外部引擎，成本高；查看已覆盖需求 |
| 7 | 结构模板 = 阶段 + 插槽树（无模板文件） | 用户要求"结构模板不含模板文件" |
| 8 | 文件扁平存 files/ | 便于搬移，避免多级目录复杂度 |
| 9 | 数据模型 v1.0.0 不兼容旧版 | 大版本升级，用户接受直接新模型 |
| 10 | 测试用 vitest + Node 原生独立脚本 | 环境限制（esbuild 无法 spawn）时用 Node 原生 TS 类型剥离跑纯逻辑 |
